<!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
    </head>
    <body>
        <br>
        <div class="row justify-content-center align-items-center h-100">
            <div class="col col-sm-6 col-md-6 col-lg-4 col-xl-3">
            <form action="/editprofile" method="post">
                <br>
                <div class="form-group">
                    <input type="text" value="<%= @current_user.name %>" name="fullname" class="form-control" placeholder="Full name" required="required">
                </div>
               
                <div class="form-group">
                    <input type="text" value="<%= @current_user.email %>" name="email" class="form-control" placeholder="Email" required="required">
                </div>
                
                <% if @erroremail %>
                    <p class="error text-danger"><%= @erroremail %></p>
                <% end %>
               
               <div class="form-group">
                    <input type="text" value="<%= @current_user.username %>" name="username" class="form-control" placeholder="Username" required="required">
                </div>
               
                <% if @errorusername %>
                    <p class="error text-danger"><%= @errorusername %></p>
                <% end %>

                <h5> Confirm your password <h5>

                <div class="form-group">
                    <input type="password" name="password" class="form-control" placeholder="Password" required="required">
                </div>
                
                <% if @errorpassword %>
                    <p class="error text-danger"><%= @errorpassword %></p>
                <% end %>
                
                <div class="form-group">
                    <button type="submit" class="btn btn-primary btn-block">Edit</button>
                </div>       
            
            </form>
        </div>
    </body>
</html>
