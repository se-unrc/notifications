<!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
    </head>
    <body>
            <br>
        <div class="row justify-content-center align-items-center h-100">
            <div class="col col-sm-6 col-md-6 col-lg-4 col-xl-3">
            <form action="/unsubscribe" method="post">
                <h2 class="text-center">UNSUBSCRIBE TO A CATEGORY</h2>       
                <div class="form-group">
                    Select one: <select id="category" name="category" class="custom-select">
                            <% @categories.each do |category| %>
                            <option value=<%= category.name %>> <%= category.name %> </option>
                            <% end %>
                        </select></p>
                <% if @error %>
                <p class="error text-danger"><%= @error %></p>
                <% end %>
                <% if @success %>
                 <p class="error text-success"><%= @success %></p>
                <% end %>

                <div class="form-group">
                    <button type="submit" class="btn btn-primary btn-block">Unsubscribe</button>
                </div>        
            </form> <br><br><br>
            </div>
        </div>
    </body>
</html>
